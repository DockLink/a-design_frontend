import { ProjectEmptyTab } from "@/components/projects/project-empty-tab";

export default function ProjectMinutesPage() {
  return (
    <ProjectEmptyTab
      title="Minutes"
      message="Meeting minutes are not available yet — no V2 API endpoint exists for this feature."
    />
  );
}
