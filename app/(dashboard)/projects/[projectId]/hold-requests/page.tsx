import { ProjectEmptyTab } from "@/components/projects/project-empty-tab";

export default function ProjectHoldRequestsPage() {
  return (
    <ProjectEmptyTab
      title="Hold Requests"
      message="Hold requests are not available yet — no V2 API endpoint exists for this feature."
    />
  );
}
